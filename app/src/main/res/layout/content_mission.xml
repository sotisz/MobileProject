<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/ivCheckbox"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/checkboxicon_foreground"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvMissionList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2km 이상 걷기"
        app:layout_constraintBottom_toBottomOf="@+id/ivCheckbox"
        app:layout_constraintLeft_toRightOf="@+id/ivCheckbox"
        app:layout_constraintTop_toTopOf="@+id/ivCheckbox"
        android:layout_margin="20dp"
        android:textSize="20sp"/>


</androidx.constraintlayout.widget.ConstraintLayout>